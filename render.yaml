services:
  - type: web
    name: allen-data-hub-api
    runtime: node
    plan: standard
    buildCommand: cd backend/api-server && pnpm install --include=dev --frozen-lockfile && pnpm build
    startCommand: cd backend/api-server && pnpm exec node --enable-source-maps ./dist/index.mjs
    envVars:
      - key: ENABLE_PNPM
        value: "true"
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        fromDatabase:
          name: allen-datahub-db
          property: connectionString
      - key: SESSION_SECRET
        generateValue: true
      - key: JWT_SECRET
        scope: all
      - key: JWT_EXPIRY
        value: "7d"
      - key: LOG_LEVEL
        value: info
      - key: FRONTEND_URL
        value: https://kheliss-data-hub-kheliss-data-hub.vercel.app
        scope: all
      - key: CORS_ORIGIN
        value: http://localhost:5173,http://localhost:5174,http://localhost:3000,https://kheliss-data-hub.vercel.app,https://kheliss-data-hub-kheliss-data-hub.vercel.app,https://kheliss-data-hub.onrender.com
        scope: all
      - key: BACKEND_URL
        value: https://kheliss-data-hub.onrender.com
        scope: all
      - key: PAYSTACK_PUBLIC_KEY
        scope: all
      - key: PAYSTACK_SECRET_KEY
        scope: all
      - key: PORTAL02_API_KEY
        scope: all
      - key: PORTAL02_BASE_URL
        value: https://www.portal-02.com/api/v1
        scope: all
      - key: BREVO_API_KEY
        scope: all

databases:
  - name: ewura-hub-db
    plan: standard
    databaseName: ewura-hub-prod
    ipAllowList:
      - source: 0.0.0.0/0
        description: Allow all IPs

autoDeploy: true
